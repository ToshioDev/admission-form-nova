import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Sistema de Admisión - Academia Nova",
  description: "Bienvenido al Sistema de Admisión de Academia Nova, donde facilitamos el proceso de registro para nuevos estudiantes. Nuestra plataforma intuitiva permite a los usuarios completar su solicitud de manera rápida y eficiente, asegurando que cada detalle sea atendido. Únete a nosotros y da el primer paso hacia tu futuro académico.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
